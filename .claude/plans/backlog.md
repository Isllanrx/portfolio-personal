# Backlog

## Prioridade média
- [ ] Gerar OG image real 1200x630 (`opengraph-image.tsx`) — hoje usa `perfil.webp` retrato, proporção errada para preview social
- [ ] Adicionar favicon `.ico`/`.svg` dedicado (hoje só `logo.webp` como icon)
- [ ] Avaliar mover `<html lang>` para SSR correto por locale (hoje corrigido via `LangSetter` no client)

## Prioridade baixa
- [ ] Migrar ESLint para v9 quando `eslint-config-next` estabilizar suporte flat completo
- [ ] Formulário de contato: considerar envio via API route com rate limiting além do deep-link do WhatsApp
